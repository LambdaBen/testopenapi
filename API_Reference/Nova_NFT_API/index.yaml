  #NFT API
  /{protocol}/{network}/{address}/{contract}/nft:
    $ref: './getListMyNft.yaml'
  /{protocol}/{network}/{address}/{contract}/nft2:
    $ref: './getListMyNft2.yaml'
  #marketData API
  /{protocol}/{network}/{address}/{contract}/opensea/nft:
    $ref: './getOpenseaData.yaml'
  /{protocol}/{network}/{address}/{contract}/upbit/nft:
    $ref: './getUpbitData.yaml'
